# AHOD
Space engineers mod

###TODO:
* Many redundant cheks if blocks are tracked, untracked blocks rarely trigger any code
* Unify interface vs object types in function parameters
* Configurable target for printing grid status into detailed info
* Configurable global requirement scaling number
* Debug level is not read from config
* Weighted average instead of minimum when calculating efficiencies from several sources
* Congifurable exclusion from being a toggle target
* Inclusion to toggle targets without requirements -> eg. light flicker
* Buffer time until efficiency debuffs activate
* Configurable severity of unmet requirements
